package cn.fundview.app.tool;

import android.content.Context;

public class Util {

    public static String getUserId(Context context) {

//		// 先得到用户的唯一码
//		String useId = "";
//		
//		Profile profile = DaoFactory.getInstance(context).getProfileDao().getProfile(InstallationId.DRIVER_ID_LABEL);
//		if (null == profile) {
//			DaoFactory.getInstance(context).getProfileDao().saveProfile(InstallationId.DRIVER_ID_LABEL, InstallationId.getDriverId(context));
//			profile = DaoFactory.getInstance(context).getProfileDao().getProfile(InstallationId.DRIVER_ID_LABEL);
//		}
//		useId = profile.getAttrValue();
//
//		return useId;
        return null;
    }

}
